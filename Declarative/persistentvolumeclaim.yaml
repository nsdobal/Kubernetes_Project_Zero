apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-az
  namespace: ndapp
spec:
  volumeName: nd-pv
  storageClassName: ""
  accessModes:
    -  ReadWriteMany
  resources: 
    requests:
      storage: 100Gi