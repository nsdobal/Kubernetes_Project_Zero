apiVersion: v1
kind: PersistentVolume
metadata:
  name: nd-pv
  namespace: dev
spec:
  storageClassName: ""
  accessModes: 
    - ReadWriteMany
  capacity:
    storage: 100Gi
  azureFile:
    readOnly: false
    secretName: azsecret
    secretNamespace: dev
    shareName: ndfileshare